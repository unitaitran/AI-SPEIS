import React from 'react';
import ProfileModal from '../../../components/user/ProfileModal/ProfileModal';

function ProfilePage() {
  return <ProfileModal onClose={() => {}} onUserUpdated={() => {}} />;
}

export default ProfilePage;
